{
  "name": "commission-quote-api-mock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stand-in for the vendor Commission Quote API. Own HTTP process, own port.",
  "scripts": {
    "start": "tsx --env-file-if-exists=.env src/server.ts",
    "test": "vitest run tests/unit",
    "test:contract": "vitest run tests/contract",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^3.2.7",
    "zod": "^4.5.4"
  }
}
