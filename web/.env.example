# Copy to .env before `npm start`.
# `npm run dev` needs none of this — it runs against the vendor double.

# Where the vendor lives. The stand-in, for now.
VENDOR_URL=http://localhost:4000

# Must match the mock's API_KEY.
VENDOR_API_KEY=local-dev-key

# Optional. Defaults to 4001.
# PORT=4001
